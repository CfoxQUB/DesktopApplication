<Page x:Class="TimetablingClientApplication.Views.MasterViews.EventPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d"
	Title="EventPage" MinHeight="450" MinWidth="1000" Width="1000" Height="450">

    <Grid MinHeight="450" MinWidth="1000" Width="1000" Height="450">
        <Label Content="Building:" Margin="430,19,510,405" RenderTransformOrigin="-38.4,-10.7"/>
        <ComboBox Name="BuildingList" HorizontalAlignment="Left" Margin="503,23,0,0" VerticalAlignment="Top" Width="143" Background="{x:Null}" Height="22" SelectionChanged="Building_Selection_Changed"/>
        <Label Content="Room:" Margin="729,19,218,405" RenderTransformOrigin="-38.4,-10.7"/>
        <ComboBox Name="RoomList" HorizontalAlignment="Left" Margin="787,23,0,0" VerticalAlignment="Top" Width="143" Background="{x:Null}" Height="22" SelectionChanged="Room_Selection_Changed"/>
        <ListView Name="ListedEvents" HorizontalAlignment="Left"  VerticalAlignment="Top" Height="370" Width="980" Margin="10,65,0,0" >
            <ListView.View>
                <GridView TextBlock.TextAlignment="Center">
                    <GridViewColumn Width="50" DisplayMemberBinding="{Binding EventId}" Header="Id" />
                    <GridViewColumn Width="150" DisplayMemberBinding="{Binding EventTitle}" Header="Event Name" />
                    <GridViewColumn Width="150" DisplayMemberBinding="{Binding EventDescription}" Header="Description" />
                    <GridViewColumn Width="150" DisplayMemberBinding="{Binding CreateDate}" Header="Date" />
                    <GridViewColumn Width="150" DisplayMemberBinding="{Binding Time}" Header="Time" />
                    <GridViewColumn Width="150" DisplayMemberBinding="{Binding Duration}" Header="Duration" />
                    <GridViewColumn Width="150" DisplayMemberBinding="{Binding Repeats}" Header="Repeats" />
                    <GridViewColumn Width="150" DisplayMemberBinding="{Binding AdditionalNotes}" Header="Additional Notes" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
